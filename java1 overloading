// `` You are asked to design a class Laptop that demonstrates constructor overloading in Java.
// Requirements:
// The class should have the following instance variables:

// brand (String)
// model (String)
// price (double)
// ramSize (int, in GB)
// You must create three different constructors for this class:
// Constructor 1 (No-argument constructor):
// Initializes the laptop with default values (e.g., "Unknown", "Generic Model", 0.0, and 4GB RAM).
// Constructor 2 (Partial parameterized constructor)
// Accepts only brand and model, while assigning default values for price and ramSize.
// Constructor 3 (Fully parameterized constructor):

// Accepts all four values from the user and initializes the object accordingly.
// Write a displayDetails() method that prints all the details of the laptop.
// In the main method, create three different objects of Laptop using each constructor, and display their details.``



//Solution-
class Laptop{
String brand ;
String model;
double price ;
int ramSize;
  Laptop(){
    brand ="Unknown";
    model ="Generic Model";
    price =0.0;
    ramSize=4;

 }
 Laptop(String brand, String model){
    this.brand = brand ;
    this.model.model;
    this.price = 0.0;  
        this.ramSize = 4;

 }

  Laptop(String brand, String model, double price, int ramSize) {
        this.brand = brand;
        this.model = model;
        this.price = price;
        this.ramSize = ramSize;
    }
       void displayDetails() {
        System.out.println("Laptop Details:");
        System.out.println("Brand: " + brand);
        System.out.println("Model: " + model);
        System.out.println("Price: $" + price);
        System.out.println("RAM Size: " + ramSize + "GB");
        
    }

}
public class Main {
    public static void main(String[] args) {
        Laptop laptop1 = new Laptop();
        Laptop laptop2 = new Laptop("Asus", "Inspiron 9");
        Laptop laptop3 = new Laptop("Apple", "MacBook Air", 1200.50, 8);
        laptop1.displayDetails();
        laptop2.displayDetails();
        laptop3.displayDetails();
    }
}
